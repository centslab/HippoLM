# Core ML
# torch 2.12.0 requires CUDA 13.0+ (no cu128/12.6 wheel). Pair with
# cuda-toolkit-13-0 (apt) on the dev box. If using CUDA 12.x, pin
# torch to the matching cu wheel: 2.11.0+cu128 or 2.9.1+cu128.
torch==2.12.0
transformers>=5.5.4
numpy>=1.24.0
einops>=0.8.2
# Triton: used directly by src/models/ops/_triton/ and cuda/kda_fwd/,
# and heavily by the vendored FLA (src/models/ops/_vendored/fla). FLA
# floor is 3.3.0; torch 2.12 bundles a compatible triton.
triton>=3.3.0

# Data
datasets>=2.14.0
# Streaming parquet loader (RotatingParquetIterable, LocalParquetIterable)
pyarrow>=14.0.0
# MS repo-tree API discovery + part downloads (rotating.py, prefetch.py)
requests>=2.31.0
# MS dataset streaming (load_streaming_with_fallback in sources.py)
modelscope>=1.18.0

# Evaluation
evalscope>=1.6.0

# Testing
pytest>=7.0.0

# Utilities
pyyaml>=6.0
